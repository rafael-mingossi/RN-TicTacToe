import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#dafaf7",
    paddingVertical: 20,
    paddingHorizontal: 25,
    borderRadius: 30,
  },
  btnTxt: { fontSize: 18, color: "#221a36", textAlign: "center" },
});
export default styles;
